'use strict';

{
let numbers = [1,2,3];
let doubled = [];

// for (let i = 0; i < numbers.length; i++) {
//   doubledNumbers.push(numbers[i] * 2);
// }

let doubl = numbers.map(function(number) {
  return number * 2;
});


console.log (doubl);





}
